# Optional. The MVP uses a deterministic Chinese fallback explanation without this key.
OPENAI_API_KEY=

# Comma-separated frontend origins. Defaults to localhost origins when empty.
CORS_ORIGINS=

# Optional future map adapters. Map Insight Lite currently uses mock data only.
GOOGLE_MAPS_API_KEY=
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=
TGOS_API_KEY=
