name: Release Candidate Quality Gate

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      VALUATION_DEMO_MODE: "false"
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements.txt
      - name: Install frontend dependencies
        run: npm ci --prefix frontend_next
      - name: Run Python tests
        run: python -m pytest -q --basetemp "${RUNNER_TEMP}/release-quality-pytest"
      - name: Build frontend
        run: npm --prefix frontend_next run build
      - name: Run release contract gate
        run: python scripts/release_quality_gate.py --skip-tests --skip-frontend-build
