import { attentionCategoryLabel, type DecisionAttentionItem } from "@/lib/decision-case-journey";

export function DecisionAttentionPanel({ items, onNavigate }: { items: readonly DecisionAttentionItem[]; onNavigate: (action: DecisionAttentionItem["action"]) => void }) {
  const ordered = [...items].sort((left, right) => ["blocked", "missing", "unknown", "partial", "pending"].indexOf(left.category) - ["blocked", "missing", "unknown", "partial", "pending"].indexOf(right.category));
  return <section aria-labelledby="decision-attention-heading" className="min-w-0 rounded-xl border border-amber-200 bg-amber-50 p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold tracking-wider text-amber-800">ATTENTION FIRST</p><h3 id="decision-attention-heading" className="mt-1 text-base font-black text-amber-950">先處理阻擋與缺失資料</h3></div><span className="text-xs font-bold text-amber-800">{ordered.length ? `${ordered.length} 項` : "目前無明確項目"}</span></div>{ordered.length ? <ul className="mt-3 grid gap-2 md:grid-cols-2">{ordered.map((item) => <li key={item.id} className="min-w-0 rounded-lg bg-white/80 p-3"><div className="flex items-start justify-between gap-2"><div className="min-w-0"><p className="text-[10px] font-bold text-amber-800">{attentionCategoryLabel(item.category)}</p><p className="mt-1 break-words text-xs font-bold text-slate-900">{item.label}</p><p className="mt-1 text-[11px] text-slate-600">{item.status}</p></div><button type="button" onClick={() => onNavigate(item.action)} className="shrink-0 rounded-lg border border-amber-300 px-2 py-1.5 text-[10px] font-bold text-amber-900">前往處理</button></div></li>)}</ul> : <p className="mt-3 text-xs leading-5 text-amber-900">尚無可評估項目；未知或尚未評估不代表沒有問題。</p>}<p className="mt-3 text-[11px] leading-5 text-amber-800">缺失、未知、部分資料與暫不可用狀態會保留原語意，不會被換算成 0 或低風險。</p></section>;
}
