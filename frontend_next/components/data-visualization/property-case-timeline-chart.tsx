import type { CaseMilestone, TimelineEvent, TimelineReadinessResult } from "@/lib/property-case-timeline";
import { CASE_MILESTONE_TEMPLATE } from "@/lib/property-case-timeline";

export function PropertyCaseTimelineChart({ events, milestones, readiness }: { events: TimelineEvent[]; milestones: CaseMilestone[]; readiness: TimelineReadinessResult }) {
  return <section className="rounded-2xl border border-stone-200 bg-white p-4" aria-label="案件時間軸與里程碑"><div><p className="text-xs font-bold text-slate-500">EXECUTIVE TIMELINE</p><h3 className="mt-1 text-sm font-black text-slate-900">時間軸與里程碑</h3><p className="mt-1 text-xs text-slate-600">只使用既有事件日期與里程碑狀態；缺少日期不會自動補今天。</p></div><div className="mt-4 rounded-xl border border-cyan-100 bg-cyan-50 p-3" role="img" aria-label="案件里程碑完成狀態"><div className="flex h-3 gap-1 overflow-hidden rounded-full bg-white">{milestones.map((milestone) => <span key={milestone.milestone_id} className={`h-full flex-1 ${milestone.is_done ? "bg-cyan-600" : "bg-slate-200"}`} />)}</div><p className="mt-2 text-xs text-cyan-900">{readiness.milestone_done_count} / {milestones.length} 個里程碑已完成；時間軸事件 {readiness.event_count} 筆。</p></div><ol className="mt-4 space-y-3">{events.length ? events.map((event) => <li key={event.id} className="rounded-xl border border-stone-200 p-3"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-stone-100 px-2 py-1 text-[10px] font-bold text-slate-700">{event.event_date || "日期未提供"}</span><strong className="text-xs text-slate-900">{event.title}</strong></div><p className="mt-1 text-[11px] leading-5 text-slate-600">{event.summary || "尚無摘要"}</p></li>) : <li className="rounded-xl border border-dashed border-stone-200 p-3 text-xs text-slate-500">尚無已輸入時間軸事件。</li>}</ol><details className="mt-4 rounded-xl border border-stone-200"><summary className="cursor-pointer px-3 py-2 text-xs font-bold text-slate-700">查看里程碑詳細狀態</summary><ul className="space-y-2 border-t border-stone-200 p-3 text-[11px] text-slate-600">{milestones.map((milestone) => <li key={milestone.milestone_id}><strong>{CASE_MILESTONE_TEMPLATE.find((item) => item.milestone_id === milestone.milestone_id)?.label ?? milestone.milestone_id}</strong>：{milestone.is_done ? "已完成" : "未完成"}{milestone.done_date ? `；日期 ${milestone.done_date}` : "；日期未提供"}</li>)}</ul></details></section>;
}
